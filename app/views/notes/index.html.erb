<div class="max-w-5xl mx-auto mt-10 px-4">
  <h1 class="text-3xl font-bold text-indigo-600 mb-6">📘 Meine Notizen</h1>

  <div class="mb-6">
    <%= link_to "➕ Neue Notiz", new_note_path, class: "bg-indigo-500 text-white px-4 py-2 rounded hover:bg-indigo-600" %>
  </div>

  <% @notes.each do |note| %>
    <div class="bg-white shadow rounded p-4 mb-4 border border-gray-200">
      <h2 class="text-xl font-semibold text-gray-800"><%= note.title %></h2>
      <p class="text-gray-600 mt-2 whitespace-pre-line"><%= note.content %></p>
    </div>
  <% end %>
</div>
